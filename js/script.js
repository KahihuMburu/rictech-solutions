function openLink(url) {
    if (url) {
        window.open(url, '_blank', 'noopener,noreferrer');
    }
}

document.addEventListener('DOMContentLoaded', () => {
    console.log('%c✅ RicTech Solutions loaded successfully!', 'color:#f5a623; font-weight:bold;');
});
